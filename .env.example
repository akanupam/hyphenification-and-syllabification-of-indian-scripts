# Environment Configuration

# Frontend (Vercel)
# API URL - set to your Render backend URL in production
VITE_API_URL=http://localhost:8000

# Backend (Render)
# Port is automatically set by Render
PORT=8000
