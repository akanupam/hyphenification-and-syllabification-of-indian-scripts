# Devanagari Syllabification API - Production
# Minimal dependencies for running the inference server

# API Server
fastapi>=0.104.0
uvicorn>=0.24.0

# Core ML (inference only)
scikit-learn>=1.3.0
numpy>=1.24.0

# PyTorch CPU-only (much smaller, no CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
